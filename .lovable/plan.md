

# Replace Book Cover Image

## Change

Overwrite `src/assets/science-of-story-cover.png` with the uploaded book cover image (`user-uploads://Screenshot_2026-03-25_at_8.40.45_AM.png`). No code changes needed since `MissionSection.tsx` already imports from that path.

